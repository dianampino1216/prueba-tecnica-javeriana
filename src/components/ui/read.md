UI: componetes reutilizables que se vayan a utilizar en la aplicación (genericos)

fuera UI: componentes especificos

# acceder a variable de entorno 
import.meta.env.API_KEY

si no funciona console.log(import.meta.env.VITE_SOME_KEY)